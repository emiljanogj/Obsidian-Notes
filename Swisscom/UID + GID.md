- To find user id we use the following command:
```
id -u {username}
```
- To find the group id we use the following command:
```
id -g {username}
```

``` 
rpm -qa --last | grep bacula
cat /etc/cron.d/autoupdate_modules
```